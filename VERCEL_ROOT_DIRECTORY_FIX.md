# 🔧 Vercel Root Directory 设置详细指南

## 问题确认

你的错误信息显示：
```
Running "install" command: `npm install`...
npm error path /vercel/path0/package.json
```

这说明 **Root Directory 设置没有生效**，Vercel 还是在根目录找文件。

---

## ✅ 正确设置 Root Directory 的步骤

### 📍 方法 1: 在 Settings 中设置（最可靠）

#### 步骤 1: 进入项目设置

1. 访问：https://vercel.com/dashboard
2. 找到并点击 `cave-exploration-game` 项目
3. 点击顶部的 **"Settings"** 标签

#### 步骤 2: 找到 Root Directory 设置

1. 在左侧菜单中，确保选中 **"General"**
2. 向下滚动，找到 **"Root Directory"** 部分
3. 你会看到类似这样的界面：

```
Root Directory
The directory within your project, in which your code is located.
Leave this field empty if your code is located in the root directory.

Current: ./                    [Edit]
```

#### 步骤 3: 编辑 Root Directory

1. 点击右侧的 **"Edit"** 按钮
2. 会出现一个输入框
3. **清空现有内容**（如果有 `./` 或其他内容）
4. 输入：`frontend`（注意：只输入 `frontend`，不要有 `/` 或 `./`）
5. 点击 **"Save"** 按钮

#### 步骤 4: 验证设置

保存后，应该显示：
```
Root Directory
Current: frontend              [Edit]
```

#### 步骤 5: 触发新部署

**重要**：修改设置后，必须触发新的部署才会生效！

1. 点击顶部的 **"Deployments"** 标签
2. 找到最新的失败部署
3. 点击右侧的 **"..."** 三个点菜单
4. 选择 **"Redeploy"**
5. 在弹出的确认框中，点击 **"Redeploy"**

---

## 📍 方法 2: 删除项目重新导入（如果方法1不行）

如果上面的方法还是不行，说明配置可能有缓存问题，需要重新导入：

### 步骤 1: 删除现有项目

1. 在项目的 **Settings** 页面
2. 滚动到最底部
3. 找到 **"Delete Project"** 部分
4. 点击 **"Delete"** 按钮
5. 输入项目名称 `cave-exploration-game` 确认
6. 点击确认删除

### 步骤 2: 重新导入项目

1. 访问：https://vercel.com/new
2. 如果需要，使用 GitHub 账号登录
3. 在 "Import Git Repository" 下找到：
   - `cathylee12127/cave-exploration-game`
4. 点击 **"Import"**

### 步骤 3: **关键配置**（这次一定要对！）

在 "Configure Project" 页面：

#### 3.1 Framework Preset
- 选择：**Vite**

#### 3.2 Root Directory（最重要！）
1. 找到 **"Root Directory"** 这一行
2. 默认显示：`./` 或为空
3. **点击右侧的 "Edit" 按钮**（这一步很重要！）
4. 在弹出的输入框中输入：`frontend`
5. 点击 **"Continue"** 或 **"Save"**

#### 3.3 Build and Output Settings
这些应该自动填写，确认是：
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

### 步骤 4: 部署

1. 确认所有配置正确
2. 点击 **"Deploy"** 按钮
3. 等待 1-2 分钟

---

## 🎯 配置对比

### ❌ 错误配置（会导致你的错误）

```
Root Directory: ./
或
Root Directory: (空)
```

Vercel 会在这里找文件：
```
/vercel/path0/package.json  ← 不存在！
```

### ✅ 正确配置

```
Root Directory: frontend
```

Vercel 会在这里找文件：
```
/vercel/path0/frontend/package.json  ← 存在！
```

---

## 📸 详细截图说明

### 在 Settings 页面

你应该看到类似这样的界面：

```
┌────────────────────────────────────────────────┐
│ Settings                                        │
├────────────────────────────────────────────────┤
│ General                                         │
│                                                 │
│ Project Name                                    │
│ cave-exploration-game                           │
│                                                 │
│ Root Directory                                  │
│ The directory within your project, in which     │
│ your code is located.                           │
│                                                 │
│ Current: ./                          [Edit] ←── 点这里
│                                                 │
└────────────────────────────────────────────────┘
```

点击 **[Edit]** 后：

```
┌────────────────────────────────────────────────┐
│ Root Directory                                  │
├────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────┐ │
│ │ frontend                                   │ │ ← 输入这个
│ └────────────────────────────────────────────┘ │
│                                                 │
│ [Cancel]  [Save] ←────────────────────────── 点这里
└────────────────────────────────────────────────┘
```

### 在 Import 页面（重新导入时）

```
┌────────────────────────────────────────────────┐
│ Configure Project                               │
├────────────────────────────────────────────────┤
│ Framework Preset                                │
│ Vite                                    [Edit]  │
│                                                 │
│ Root Directory                                  │
│ ./                                      [Edit] ←── 点这里
│                                                 │
│ Build and Output Settings                       │
│ Build Command: npm run build                    │
│ Output Directory: dist                          │
│ Install Command: npm install                    │
│                                                 │
│                              [Deploy]           │
└────────────────────────────────────────────────┘
```

---

## ✅ 成功标志

部署成功后，你会看到：

### 1. 构建日志显示正确路径

```
✓ Cloning completed
✓ Running "vercel build"
✓ Running "install" command: `npm install`...
✓ added 123 packages  ← 看到这个说明找到了 package.json
✓ Running "build" command: `npm run build`...
✓ Build completed
```

### 2. 部署状态

- 状态：**Ready** 或 **Production**
- 有一个可访问的域名

### 3. 访问网站

- 点击 **"Visit"** 或访问域名
- 能看到游戏登录界面

---

## 🔍 检查你的 GitHub 仓库

在继续之前，先确认你的 GitHub 仓库结构正确：

1. 访问：https://github.com/cathylee12127/cave-exploration-game
2. 确认你能看到这些文件夹：
   - `frontend/`
   - `backend/`
3. 点击进入 `frontend/` 文件夹
4. 确认里面有：
   - `package.json`
   - `index.html`
   - `src/` 文件夹
   - `vite.config.js` 或类似配置文件

如果这些文件都在，说明上传成功，问题就是 Vercel 配置。

---

## 🆘 如果还是不行

### 检查清单

在重新部署前，确认：

- [ ] Root Directory 设置为 `frontend`（不是 `./frontend` 或 `/frontend`）
- [ ] 已经点击 "Save" 保存设置
- [ ] 已经触发新的部署（Redeploy）
- [ ] GitHub 仓库中 `frontend/package.json` 文件存在

### 可能的其他问题

#### 问题 1: GitHub 仓库文件结构不对

**检查方法**：
访问 https://github.com/cathylee12127/cave-exploration-game/blob/main/frontend/package.json

如果这个链接打不开，说明文件上传有问题。

**解决方法**：
重新上传文件，确保 `frontend` 文件夹和里面的所有文件都上传了。

#### 问题 2: Vercel 缓存问题

**解决方法**：
1. 删除项目
2. 等待 1 分钟
3. 重新导入

#### 问题 3: 配置没有保存

**解决方法**：
1. 修改 Root Directory 后，一定要点 "Save"
2. 看到 "Settings saved" 或类似提示
3. 然后再 Redeploy

---

## 💡 为什么要设置 Root Directory？

你的项目结构：
```
cave-exploration-game/          ← GitHub 仓库根目录
├── frontend/                   ← 前端代码在这里
│   ├── package.json           ← npm 需要这个文件
│   ├── index.html
│   ├── src/
│   └── vite.config.js
├── backend/                    ← 后端代码（Vercel 不需要）
└── README.md
```

**不设置 Root Directory**：
- Vercel 在 `cave-exploration-game/` 找 `package.json`
- 找不到 → 报错 ❌

**设置 Root Directory 为 `frontend`**：
- Vercel 在 `cave-exploration-game/frontend/` 找 `package.json`
- 找到了 → 构建成功 ✅

---

## 📝 操作步骤总结

### 快速修复（推荐）

1. 进入 Vercel 项目 Settings
2. 找到 Root Directory，点击 Edit
3. 输入 `frontend`，点击 Save
4. 进入 Deployments，点击 Redeploy
5. 等待部署完成

### 完全重来（如果快速修复不行）

1. 删除 Vercel 项目
2. 重新从 GitHub 导入
3. 在配置页面，Root Directory 点击 Edit，输入 `frontend`
4. 点击 Deploy
5. 等待部署完成

---

## 🎉 下一步

部署成功后：

1. **获取域名**：复制 Vercel 给你的域名
2. **生成二维码**：运行 `node generate-fixed-qrcode.js`
3. **输入域名**：粘贴 Vercel 域名
4. **获得二维码**：在 `qrcode-fixed.png` 文件中

---

**现在试试方法 1 或方法 2，告诉我结果如何！** 😊

如果还有问题，告诉我：
1. 你选择了哪个方法？
2. 在哪一步遇到问题？
3. 看到什么错误信息？
